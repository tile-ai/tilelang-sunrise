# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
ci:
  autofix_prs: false
  autofix_commit_msg: "[Lint]: [pre-commit.ci] auto fixes [...]"
  autoupdate_commit_msg: "[CI] [pre-commit.ci] autoupdate"
  autoupdate_schedule: monthly
default_stages: [pre-commit, pre-push, manual]
# TileLang owns only the root project. Vendored dependencies and downstream
# projects are outside this pre-commit boundary.
exclude: '^(build|3rdparty|downstream)/.*$'
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0 # May not sync with requirements-lint.txt, but it's OK for now
    hooks:
      - id: check-symlinks
      - id: destroyed-symlinks
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-added-large-files
      - id: check-merge-conflict
        fail_fast: true
      - id: check-executables-have-shebangs
      - id: check-shebang-scripts-are-executable
      - id: detect-private-key
      - id: check-yaml
      - id: check-toml
      - id: check-ast
        fail_fast: true
      - id: debug-statements
      - id: file-contents-sorter
        args: [--ignore-case]
        files: ^docs/spelling_wordlist\.txt$
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v22.1.5  # sync with requirements-lint.txt
    hooks:
      - id: clang-format
        types_or: [c++, c]
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.20  # sync with requirements-lint.txt
    hooks:
      - id: ruff-check
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format
        args: [--exit-non-zero-on-format]
  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.2  # sync with requirements-lint.txt
    hooks:
      - id: codespell
        additional_dependencies: [".[toml]"]
        exclude: |
          (?x)(
            ^.+\.(cpp|hpp|cxx|cc|c|h|cu|cuh)$|
            ^.+\.svg$|
            ^.*\brequirements\b.*\.txt$
          )
  - repo: https://github.com/jackdewinter/pymarkdown
    # Last release compatible with the Python 3.9 runtime on public CI runners.
    rev: v0.9.29
    hooks:
      - id: pymarkdown
        args: ["--config", ".pymarkdown", "fix"]
        exclude: ^\.agents/
